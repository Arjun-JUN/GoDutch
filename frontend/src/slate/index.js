/**
 * ─── Slate Design System ───
 * Top-level barrel export. Import everything from '@/slate'.
 *
 * Usage:
 *   import { AppButton, AppSurface, Header } from '@/slate';
 *   import { Receipt, Plus } from '@/slate/icons';
 */
export * from './components';
